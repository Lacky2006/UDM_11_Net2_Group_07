<Window x:Class="UploadClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Upload Client"
        Height="400"
        Width="600">

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">

            <TextBlock Text="IP:"
                       VerticalAlignment="Center"/>

            <TextBox x:Name="txtIP"
                     Width="150"
                     Margin="5"
                     Text="127.0.0.1"/>

            <TextBlock Text="Port:"
                       VerticalAlignment="Center"/>

            <TextBox x:Name="txtPort"
                     Width="100"
                     Margin="5"
                     Text="9000"/>

        </StackPanel>

        <Button Grid.Row="1"
                Height="40"
                Margin="0,10,0,10"
                Content="Send Ping"
                Click="btnPing_Click"/>

        <TextBox x:Name="txtLog"
                 Grid.Row="2"
                 IsReadOnly="True"
                 AcceptsReturn="True"/>

    </Grid>

</Window>