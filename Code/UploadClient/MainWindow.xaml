<Window x:Class="UploadClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Upload Client" Height="400" Width="600">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBlock Text="IP:" VerticalAlignment="Center"/>
            <TextBox x:Name="txtIP" Width="150" Margin="5" Text="127.0.0.1"/>
            <TextBlock Text="Port:" VerticalAlignment="Center"/>
            <TextBox x:Name="txtPort" Width="100" Margin="5" Text="9000"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,10,0,10">
            <Button x:Name="btnConnect" Content="Kết nối tới Server" Click="btnConnect_Click" Height="30" Margin="0,0,0,5"/>
            <Button x:Name="btnPing" Content="Send Ping" Click="btnPing_Click" Height="40"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <TextBox x:Name="txtLog" IsReadOnly="True" AcceptsReturn="True"/>
        </ScrollViewer>
    </Grid>
</Window>