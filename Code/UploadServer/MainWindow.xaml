<Window x:Class="UploadServer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Upload Server"
        Height="600"
        Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}">

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition Width="12" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Left Sidebar -->
        <Border Grid.Column="0" CornerRadius="8" Padding="12" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="UPLOAD SERVER" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,12" />

                <!-- Server status card -->
                <StackPanel Margin="0,0,0,10">
                    <TextBlock Text="Server" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" />
                    <Border CornerRadius="6" Padding="10" Background="{DynamicResource SurfaceBrush}" Margin="0,6,0,0">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,8">
                                <Ellipse Width="12" Height="12" Fill="{DynamicResource SuccessBrush}" x:Name="serverStatusDot" />
                                <TextBlock Text=" Running" FontWeight="SemiBold" Margin="8,0,0,0" />
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,4,0,4">
                                <TextBlock Text="IP:" Width="40" VerticalAlignment="Center" />
                                <TextBox x:Name="txtIP" Width="140" Height="30" Text="0.0.0.0" VerticalContentAlignment="Center" Background="{DynamicResource InputBackgroundBrush}" BorderBrush="{DynamicResource InputBorderBrush}" Foreground="{DynamicResource InputForegroundBrush}" />
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,4,0,8">
                                <TextBlock Text="Port:" Width="40" VerticalAlignment="Center" />
                                <TextBox x:Name="txtPort" Width="100" Height="30" Text="9000" VerticalContentAlignment="Center" Background="{DynamicResource InputBackgroundBrush}" BorderBrush="{DynamicResource InputBorderBrush}" Foreground="{DynamicResource InputForegroundBrush}" />
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <Button x:Name="btnStart" Content="Start Server" Width="100" Height="32" Click="btnStart_Click" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,0" />
                                <Button x:Name="btnStop" Content="Stop Server" Width="103" Height="32" IsEnabled="False" Click="btnStop_Click" Style="{StaticResource SecondaryButtonStyle}" />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Statistics summary -->
                <TextBlock Text="Overview" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,8,0,6" />
                <StackPanel>
                    <Border CornerRadius="6" Padding="10" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Margin="0,0,0,8">
                        <StackPanel>
                            <TextBlock Text="Connected Clients" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" />
                            <TextBlock x:Name="lblClientsCount" Text="0" FontSize="20" FontWeight="Bold" Margin="0,6,0,0" />
                        </StackPanel>
                    </Border>

                    <Border CornerRadius="6" Padding="10" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="Active Transfers" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" />
                            <TextBlock x:Name="lblTransfersCount" Text="0" FontSize="20" FontWeight="Bold" Margin="0,6,0,0" />
                        </StackPanel>
                    </Border>
                </StackPanel>

            </StackPanel>
        </Border>

        <!-- Center content -->
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="8" />
                <RowDefinition Height="2*" />
                <RowDefinition Height="8" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Top: title -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Row="0" HorizontalAlignment="Stretch">
                <TextBlock Text="Dashboard" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,0" />
            </StackPanel>

            <!-- Middle: Clients and Transfers -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Connected Clients -->
                <Border Grid.Column="0" CornerRadius="8" Padding="10" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Connected Clients" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8" />
                        <ListView x:Name="lvClients" Height="260" Background="{DynamicResource SurfaceBrush}" BorderThickness="0" Foreground="{DynamicResource DataGridForegroundBrush}">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="120" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Client" />
                                        <TextBlock Grid.Column="1" Text="Connected" HorizontalAlignment="Right" Foreground="{DynamicResource TextSecondaryBrush}" />
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </Border>

                <!-- Receiving Files -->
                <Border Grid.Column="2" CornerRadius="8" Padding="10" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Receiving Files" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8" />
                        <DataGrid x:Name="dgTransfers" Height="260" AutoGenerateColumns="False" HeadersVisibility="Column" CanUserAddRows="False" IsReadOnly="True" GridLinesVisibility="None" RowBackground="{DynamicResource SurfaceBrush}" AlternatingRowBackground="{DynamicResource SurfaceAltBrush}" Foreground="{DynamicResource DataGridForegroundBrush}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="File" Binding="{Binding FileName}" Width="*" />
                                <DataGridTextColumn Header="Client" Binding="{Binding Client}" Width="150" />
                                <DataGridTextColumn Header="Size" Binding="{Binding Size}" Width="100" />
                                <DataGridTemplateColumn Header="Progress" Width="160">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <ProgressBar Value="{Binding Progress}" Minimum="0" Maximum="100" Height="12" Style="{StaticResource SmallProgressBar}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Bottom: Logs and Stats -->
            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <!-- Activity Log: reduced width -->
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="12" />
                    <!-- Statistics: increased by ~20% (3:2 ratio) -->
                    <ColumnDefinition Width="2*" />
                </Grid.ColumnDefinitions>

                <!-- Activity Log -->
                <Border Grid.Column="0" CornerRadius="8" Padding="10" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Activity Log" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8" />
                        <TextBox x:Name="txtLog" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="13" Background="{DynamicResource SurfaceBrush}" BorderThickness="0" Height="180" Foreground="{DynamicResource LogForegroundBrush}" />
                    </StackPanel>
                </Border>

                <!-- Statistics Dashboard (2x2 cards) -->
                <Border Grid.Column="2" CornerRadius="8" Padding="14" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Statistics" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,12" />

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="1*" />
                                <RowDefinition Height="1*" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*" />
                                <ColumnDefinition Width="1*" />
                            </Grid.ColumnDefinitions>

                            <!-- Card template: use Borders to ensure equal sizing -->
                            <Border Grid.Row="0" Grid.Column="0" Margin="0,0,12,12" CornerRadius="8" Padding="16" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="Total Files" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" />
                                    <TextBlock x:Name="lblTotalFiles" Text="0" Grid.Row="1" FontSize="30" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Center" HorizontalAlignment="Left" />
                                </Grid>
                            </Border>

                            <Border Grid.Row="0" Grid.Column="1" Margin="12,0,0,12" CornerRadius="8" Padding="16" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="Total Bytes" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" />
                                    <TextBlock x:Name="lblTotalBytes" Text="0 B" Grid.Row="1" FontSize="30" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Center" HorizontalAlignment="Left" />
                                </Grid>
                            </Border>

                            <Border Grid.Row="1" Grid.Column="0" Margin="0,12,12,0" CornerRadius="8" Padding="16" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="Avg Speed" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" />
                                    <TextBlock x:Name="lblAvgSpeed" Text="0 B/s" Grid.Row="1" FontSize="30" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Center" HorizontalAlignment="Left" />
                                </Grid>
                            </Border>

                            <Border Grid.Row="1" Grid.Column="1" Margin="12,12,0,0" CornerRadius="8" Padding="16" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="Uptime" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" />
                                    <TextBlock x:Name="lblUptime" Text="00:00:00" Grid.Row="1" FontSize="30" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Center" HorizontalAlignment="Left" />
                                </Grid>
                            </Border>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>
