<Window x:Class="UploadServer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Upload Server"
        Height="550"
        Width="850"
        WindowStartupLocation="CenterScreen"
        Background="#F4F6F8">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="UPLOAD SERVER"
                   FontSize="24"
                   FontWeight="Bold"
                   Foreground="#2C3E50"
                   Margin="0,0,0,20"/>

        <Border Grid.Row="1"
                Background="White"
                CornerRadius="8"
                Padding="15"
                Margin="0,0,0,15"
                BorderBrush="#DADDE1"
                BorderThickness="1">

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Server IP:" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="txtIP" Width="150" Height="35" IsReadOnly="True" Background="#EFEFEF" FontSize="15" VerticalContentAlignment="Center" Margin="0,0,15,0"/>

                <TextBlock Text="Port:" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="txtPort" Width="100" Height="35" Text="9000" MaxLength="5" FontSize="15" VerticalContentAlignment="Center" Margin="0,0,15,0" PreviewTextInput="txtPort_PreviewTextInput"/>

                <Button x:Name="btnStart" Content="Start Server" Width="130" Height="35" FontSize="15" Margin="0,0,10,0" Click="btnStart_Click"/>
                <Button x:Name="btnStop" Content="Stop Server" Width="130" Height="35" FontSize="15" IsEnabled="False" Click="btnStop_Click"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4.5*"/>
                <ColumnDefinition Width="3.5*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="0,0,15,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1.2*"/>
                    <RowDefinition Height="1.8*"/>
                </Grid.RowDefinitions>

                <GroupBox Grid.Row="0" Header="CLIENT ĐANG KẾT NỐI" FontSize="13" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15">
                    <DataGrid x:Name="dgvClients" AutoGenerateColumns="False" IsReadOnly="True" Background="White" RowHeaderWidth="0" GridLinesVisibility="Horizontal">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="STT" Binding="{Binding STT}" Width="60"/>
                            <DataGridTextColumn Header="IP Client" Binding="{Binding IPClient}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </GroupBox>

                <GroupBox Grid.Row="1" Header="TRẠNG THÁI FILE ĐÃ NHẬN" FontSize="13" FontWeight="Bold" Foreground="#2C3E50">
                    <DataGrid x:Name="dgvFiles" AutoGenerateColumns="False" IsReadOnly="True" Background="White" RowHeaderWidth="0" GridLinesVisibility="Horizontal">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Tên File" Binding="{Binding FileName}" Width="2*"/>
                            <DataGridTextColumn Header="Thời Gian" Binding="{Binding Time}" Width="1.2*"/>
                            <DataGridTextColumn Header="Trạng Thái" Binding="{Binding Status}" Width="1.2*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </GroupBox>
            </Grid>

            <GroupBox Grid.Column="1" Header="LOG HOẠT ĐỘNG" FontSize="13" FontWeight="Bold" Foreground="#2C3E50">
                <TextBox x:Name="txtLog"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         FontFamily="Consolas"
                         FontSize="13"
                         Background="#FAFAFA"
                         Padding="5"/>
            </GroupBox>
        </Grid>
    </Grid>
</Window>
